<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.zte.zakker.find">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StoreDeviceChooseActivity"
            android:label="StoreDeviceChooseActivity">
        </activity>
        <activity
            android:name=".StoreDeviceChooseDetailActivity"
            android:label="StoreDeviceChooseDetailActivity">
        </activity>
        <activity
            android:name=".ShoppingCartActivity"
            android:label="ShoppingCartActivity">
        </activity>

    </application>

</manifest>